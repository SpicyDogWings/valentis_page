---
import { Image } from "astro:assets";
---

<div class="header">
  <h1>Loviu</h1>
  <p>¿Quieres ser mi valentina?</p>
  <Image
    src="/img/heart_export.webp"
    alt="Una imagen de un corazón pixelart morado"
    width="50"
    height="50"
  />
</div>
<a href="/confirm">Sí</a>

<style scoped>
  .header {
    margin: 30vh 0 0 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    position: relative;

    h1,
    p {
      width: 100%;
      text-align: center;
      color: var(--fr);
    }

    h1 {
      font-family: "NewTitle-Bold", serif;
      font-size: 17rem;
    }

    p {
      transform: translateY(-3rem);
      font-family: "Satoshi-Regular", serif;
      font-size: 1.5rem;
    }

    img {
      position: absolute;
      transform: scale(6) translateY(-2rem) rotate(30deg);
      animation-name: example;
      animation-duration: 2s;
      animation-iteration-count: infinite;
    }
  }

  a {
    font-family: "Satoshi-Bold", serif;
    margin-top: 1rem;
    padding: 0.5rem 4rem;
    font-size: 1.7rem;
    color: var(--fr);
    background-color: var(--bg);
    border: 0.2rem solid var(--fr);
    border-radius: 1.5rem;
    box-shadow: 0.5rem 0.5rem 0 var(--fr);
    text-decoration: none;
    transition: all;
    transition-duration: 150ms;

    &:hover {
      cursor: pointer;
      transform: scale(0.95);
    }

    &:active {
      transform: scale(0.9);
    }
  }

  @keyframes example {
    0% {
      transform: scale(4) rotate(30deg);
    }
    50% {
      transform: scale(3) rotate(30deg);
    }
    100% {
      transform: scale(4) rotate(30deg);
    }
  }
</style>
